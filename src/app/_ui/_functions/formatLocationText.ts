// import {LocationDto} from "@/lib/api/locations/locationsApiDtos";
//
// export default function formatLocationText(location: LocationDto) {
//     return `${location.name}, ${location.subdivisions.join(",")}`;
// }